machine:
  timezone:
    Europe/Amsterdam
  python:
    version: 3.5.1
dependencies:
  pre:
    - pip install -r requirements.txt -q
    - pip install -r test-requirements.txt -q
test:
    override:
          - ./test.sh
