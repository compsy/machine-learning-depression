machine:
  timezone:
    Europe/Amsterdam
  python:
    version: 3
dependencies:
  override:
    - pip install -r requirements.txt -q
    - pip install -r test-requirements.txt -q
test:
    override:
          - ./test.sh
